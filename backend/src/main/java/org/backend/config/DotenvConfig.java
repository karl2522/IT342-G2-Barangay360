package org.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DotenvConfig {
    // The spring-dotenv library automatically loads .env files from the classpath
} 